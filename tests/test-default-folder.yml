name: Test default folder (workspace)
on:
  workflow_dispatch:
jobs:
  test:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@de0fac2e4500dabe0009e67214ff5f5447ce83dd # v6.0.2

      - name: Run action with default folder (github.workspace)
        id: action
        uses: .
        # No 'folder' input — defaults to ${{ github.workspace }}

      - name: Assert action completed successfully on default workspace
        run: |
          echo "Action completed successfully using default workspace folder"
          echo "PASS"
